services:
  counter:
    build:
      context: ./counterService
      dockerfile: Dockerfile
    ports:
      - 3001:3001
    volumes:
      - ./data:/counterService/data
  main:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    depends_on:
      - counter